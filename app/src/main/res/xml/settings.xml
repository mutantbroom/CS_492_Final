<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory android:title="@string/pref_weather_search">
        <EditTextPreference
            android:title="@string/pref_location_title"
            android:key="@string/pref_location_key"
            app:useSimpleSummaryProvider="true"/>

        <ListPreference
            android:title="@string/pref_unit_title"
            android:key="@string/pref_unit_key"
            android:entries="@array/pref_unit_entries"
            android:entryValues="@array/pref_unit_values"
            android:defaultValue="@string/pref_unit_default"
            android:dialogTitle="@string/pref_unit_title"
            app:useSimpleSummaryProvider="true"/>
    </PreferenceCategory>
</PreferenceScreen>